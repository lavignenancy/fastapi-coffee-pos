[project]
name = "pos"
version = "0.1.0"
description = "A Coffee shop POS"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.140.7",
    "psycopg2-binary>=2.9.12",
    "sqlalchemy>=2.0.51",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
]
